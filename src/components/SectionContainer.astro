---
const { class: className, id, maxWidth } = Astro.props;
---

<section
  id={id}
  data-section={id}
  class={`section ${className} scroll-m-20 w-full mx-auto container ${maxWidth || "lg:max-w-4xl md:max-w-2xl"}`}
>
  <slot />
</section>
