export interface SkillGroup {
    category: string;
    items: string[];
    category_en?: string;
    category_pt?: string;
}

export interface Achievement {
    title: string;
    icon: string;
    color: string;
    title_en?: string;
    title_pt?: string;
}
